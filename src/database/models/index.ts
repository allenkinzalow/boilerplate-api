import Administrator from "./Administrator";
import Config from "./Config";
import Session from "./Session";
import User from "./User";

export const models = {
  Administrator,
  Config,
  Session,
  User,
};

export { Administrator, Config, Session, User };

export default models;
